# Claude API key for the agents. Without it, sign-up and onboarding work but agent runs fail with a clear message.
ANTHROPIC_API_KEY=

# Postgres connection string for production (Neon, Supabase, RDS...).
# Leave empty locally: Budera then uses an embedded Postgres (PGlite) stored in ./.data
DATABASE_URL=
